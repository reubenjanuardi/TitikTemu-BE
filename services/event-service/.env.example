# Event Service Environment Variables
# Copy to .env and configure

# Service Configuration
PORT=3002
NODE_ENV=development

# Database (PostgreSQL via Supabase - event_schema)
DATABASE_URL=postgresql://user:password@host:5432/database?schema=event_schema

# Auth Service URL (for token validation)
AUTH_SERVICE_URL=http://localhost:3001
