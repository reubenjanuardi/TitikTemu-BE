# Attendance Service Environment Variables
# Copy to .env and configure

# Service Configuration
PORT=3003
NODE_ENV=development

# Database (PostgreSQL via Supabase - attendance_schema)
DATABASE_URL=postgresql://user:password@host:5432/database?schema=attendance_schema

# Event Service URL (for validating registrations)
EVENT_SERVICE_URL=http://localhost:3002
